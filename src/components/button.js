import React from 'react';

export default (props) => {
  return (
    <button onClick={props.action} className={props.customStyle}>                                                                                                                                                                                                                                                                                                                                                                                                                                                             ' id={props.id}>
      {props.name}
    </button>
  )
}
